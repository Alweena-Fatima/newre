//export all the dart file in the models folder
export 'quiz_model.dart'; //we dont need to give the path because all are in the same folder
export 'question_model.dart'; //we dont need to give the path because all are in the same
export 'answer_model.dart';
